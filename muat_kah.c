#include <stdio.h>

int main() {
    long long int a, b, c, d, e;
    scanf("%lld %lld %lld %lld %lld", &a, &b, &c, &d, &e);

    long long int hasil = a + b + c + d + e;
    printf("%lld\n", hasil);

    return 0;
}
